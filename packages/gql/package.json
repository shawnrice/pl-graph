{
  "name": "@lenke/gql",
  "description": "An ISO-GQL (ISO/IEC 39075) query engine that parses and executes graph queries against a @lenke/core graph in TypeScript.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "default": "./dist/esm/index.mjs"
    }
  },
  "scripts": {
    "build": "bun build.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@lenke/core": "workspace:*",
    "@lenke/errors": "workspace:*",
    "@lenke/fp": "workspace:*"
  },
  "devDependencies": {
    "@lenke/dev": "workspace:*",
    "@types/bun": "^1.4.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^6.0.0"
  }
}
